# Model training, evaluation and development tooling.
# Installs the runtime requirements first so the two environments cannot drift.
-r requirements.txt

scikit-learn==1.7.*
pandas==2.3.*
matplotlib==3.10.*
seaborn==0.13.*
ipykernel==6.*
ruff==0.14.*
